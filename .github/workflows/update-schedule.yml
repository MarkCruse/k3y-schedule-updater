name: Update K3Y Schedule

on:
  schedule:
    - cron:  '0,13,28,43 * * * *'  # run at every quarter-hour
  workflow_dispatch:                # allows manual trigger

jobs:
  update-schedule:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run scraper
      run: |
        set -e
        python scraper.py
        echo "Contents of data folder:"
        ls -la data || echo "data folder does not exist!"


    - name: Commit updated JSON
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "GitHub Actions"
        git add data/schedule-cache.json
        # Only commit if there are changes
        if ! git diff --cached --quiet; then
          git commit -m "Update schedule-cache.json [skip ci]"
          git push
        else
          echo "No changes to commit"
        fi
